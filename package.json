{
  "name": "electricpen",
  "description": "ElectricPen provides a list of blogs, allows users to create a profile, post and comment on blogs.",
  "version": "0.1.0",
  "engines": {
    "node": "12.13.0"
  },
  "private": true,
  "dependencies": {
    "bootstrap": "^4.4.1",
    "react": "^16.11.0",
    "react-bootstrap": "^1.0.0-beta.16",
    "react-dom": "^16.11.0",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:8000"
}
